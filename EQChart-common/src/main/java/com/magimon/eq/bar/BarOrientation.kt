package com.magimon.eq.bar

/**
 * Bar orientation inside chart geometry.
 *
 * @property VERTICAL renders categories on x-axis and values on y-axis.
 * @property HORIZONTAL renders categories on y-axis and values on x-axis.
 */
enum class BarOrientation {
    VERTICAL,
    HORIZONTAL,
}

